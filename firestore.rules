rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY: เปิดเพื่อให้ทดสอบ Version 1 ได้ทันที
    // ก่อนใช้งานจริงต้องเปลี่ยนเป็น Firebase Authentication + Role
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
