import { dataService, usingDemoMode } from "./data-service.js?v=20260718-021";
import { money, formatTime } from "./ui.js?v=20260805-081";
import { watchRecords, RetailCollections, setTenantId } from "./retail-db.js?v=20260731-081";
import translations from "./sales-report-translations.js?v=20260903-244";
import { configureI18n, applyTranslations, t, getLocale, getIntlLocale, formatNumber } from "./i18n.js?v=20260903-202";
import { effectiveDeliveryAmounts, enrichDeliveryGiftItems } from "./delivery-order-display.js?v=20260903-244";

configureI18n(translations);
applyTranslations();
document.title = t("sales_report.meta.title");

const backIcon = document.querySelector('.app-header a[href="/admin/"] .bi:not(.app-icon)');
if (backIcon) backIcon.remove();

if (usingDemoMode) { const banner=document.querySelector("#demoBanner"); if(banner)banner.innerHTML=`<div class="demo-banner">${t("sales_report.demo")}</div>`; }

const locale=getIntlLocale(),isThai=getLocale()==="th";
const state = { period: "daily", orders: [], retailSales: [], receipts: [], filteredReceipts: [], page: 1, pageSize: 20 };
let reportMenus = [];
const els = { reportDate:document.querySelector("#reportDate"),reportMonth:document.querySelector("#reportMonth"),reportYear:document.querySelector("#reportYear"),startDate:document.querySelector("#startDate"),endDate:document.querySelector("#endDate"),orderType:document.querySelector("#orderTypeFilter"),paymentMethod:document.querySelector("#paymentMethodFilter"),search:document.querySelector("#receiptSearch"),totalSales:document.querySelector("#totalSales"),receiptCount:document.querySelector("#receiptCount"),averageReceipt:document.querySelector("#averageReceipt"),soldItemCount:document.querySelector("#soldItemCount"),orderTypeSummary:document.querySelector("#orderTypeSummary"),paymentSummary:document.querySelector("#paymentSummary"),chartTitle:document.querySelector("#chartTitle"),chartSubtitle:document.querySelector("#chartSubtitle"),salesChart:document.querySelector("#salesChart"),topItems:document.querySelector("#topItems"),bestPeriod:document.querySelector("#bestPeriod"),receiptRows:document.querySelector("#receiptRows"),receiptResultCount:document.querySelector("#receiptResultCount"),receiptRangeLabel:document.querySelector("#receiptRangeLabel"),pagination:document.querySelector("#receiptPagination"),dialog:document.querySelector("#receiptDialog"),detail:document.querySelector("#receiptDetail") };

function setChartTitle(text){if(els.chartTitle)els.chartTitle.innerHTML=`<i class="bi bi-bar-chart-steps" aria-hidden="true"></i><span>${escapeHtml(text)}</span>`}
function toDate(value){if(!value)return null;if(typeof value.toDate==="function")return value.toDate();if(value.seconds)return new Date(value.seconds*1000);const date=new Date(value);return Number.isNaN(date.getTime())?null:date}
function localDateKey(date){return`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,"0")}-${String(date.getDate()).padStart(2,"0")}`}
function monthKey(date){return`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,"0")}`}
function startOfDay(value){const date=new Date(`${value}T00:00:00`);return Number.isNaN(date.getTime())?null:date}
function endOfDay(value){const date=new Date(`${value}T23:59:59.999`);return Number.isNaN(date.getTime())?null:date}
function escapeHtml(value=""){return String(value).replace(/[&<>"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"})[char])}
function paidDate(order){return toDate(order.paidAt)||toDate(order.completedAt)||toDate(order.updatedAt)||toDate(order.createdAt)}
function isPaidOrder(order){return order?.paymentStatus === "paid" || order?.status === "paid"}
function isCancelledItem(item){return item?.cancelled === true}
function normalizeRetailSale(sale){
  const paidAt=toDate(sale.paidAt)||toDate(sale.completedAt)||toDate(sale.createdAt)||toDate(sale.updatedAt)||new Date();
  const paymentMethod=sale?.payment?.method||sale.paymentMethod||"cash";
  return{
    ...sale,
    id:String(sale.id||sale.saleNumber||sale.number||""),
    receiptNumber:sale.receiptNumber||sale.saleNumber||sale.number||sale.id,
    orderType:sale.orderType||"table",
    tableCode:sale.tableCode||sale.registerName||"POS",
    paymentStatus:"paid",
    status:"paid",
    paymentMethod,
    paidAt,
    subtotalAmount:Number(sale.subtotalAmount??sale.subtotal??0),
    discountAmount:Number(sale.discountAmount??sale.discount??0)+Number(sale.pointDiscount||0),
    totalAmount:Number(sale.totalAmount??sale.total??0),
    items:(sale.items||[]).map(item=>({...item,name:item.name||item.productName||t("sales_report.receipt.item_fallback"),qty:Number(item.qty||0),price:Number(item.price||0)})),
    _reportSource:"retail-pos"
  }
}
function rebuildReceipts(){
  const rows=new Map();
  state.orders.forEach(order=>rows.set(`order:${order.id}`,order));
  state.retailSales.map(normalizeRetailSale).forEach(sale=>{
    const duplicate=[...rows.values()].some(order=>String(order.id||"")===sale.id||String(order.receiptNumber||"")===String(sale.receiptNumber||""));
    if(!duplicate)rows.set(`sale:${sale.id}`,sale);
  });
  state.receipts=buildReceipts([...rows.values()]);
  applyFilters();
}
function orderNet(order){if(Number.isFinite(Number(order.totalAmount)))return Number(order.totalAmount||0);return(order.items||[]).filter(item=>!isCancelledItem(item)).reduce((sum,item)=>sum+Number(item.price||0)*Number(item.qty||0),0)+Number(order.deliveryFee||0)}
function paymentMethodKey(order){const value=String(order.paymentMethod||"").toLowerCase();if(["cash","เงินสด"].includes(value))return"cash";if(["promptpay","transfer","bank","qr"].includes(value))return"promptpay";if(value==="cod")return"cod";return"other"}
function paymentMethodLabel(key){return({cash:t("sales_report.payment.cash"),promptpay:t("sales_report.payment.promptpay"),cod:t("sales_report.payment.cod"),other:t("sales_report.payment.other")})[key]||t("sales_report.payment.other")}
function orderTypeLabel(key){return key==="delivery"?t("sales_report.order_type.delivery"):t("sales_report.order_type.storefront")}
function receiptGroupKey(order){if (order.receiptNumber) return `receipt:${order.receiptNumber}`;if (order.billId) return `bill:${order.billId}`;if (order.paymentGroupId) return `group:${order.paymentGroupId}`;if(order.orderType!=="delivery"&&order.tableToken && order.paidAt)return`table:${order.tableToken}:${String(order.paidAt)}`;return`order:${order.id}`}
function receiptCode(receipt){const explicit=receipt.orders.find(order=>order.receiptNumber)?.receiptNumber;if(explicit)return String(explicit);const date=receipt.paidAt||new Date(),stamp=`${String(date.getFullYear()).slice(-2)}${String(date.getMonth()+1).padStart(2,"0")}${String(date.getDate()).padStart(2,"0")}`;return`R${stamp}-${String(receipt.orders[0]?.id||"").slice(-6).toUpperCase()}`}
function buildReceipts(orders){const groups=new Map();orders.filter(isPaidOrder).map(order=>enrichDeliveryGiftItems(order,reportMenus)).forEach(order=>{const key=receiptGroupKey(order);if(!groups.has(key))groups.set(key,[]);groups.get(key).push(order)});return[...groups.values()].map(group=>{const sorted=[...group].sort((a,b)=>(paidDate(a)?.getTime()||0)-(paidDate(b)?.getTime()||0)),first=sorted[0],items=sorted.flatMap(order=>(order.items||[]).filter(item=>!isCancelledItem(item)).map(item=>({...item,orderId:order.id}))),subtotal=sorted.reduce((sum,order)=>sum+(order.orderType==="delivery"?effectiveDeliveryAmounts(order).subtotal:Number(order.subtotalAmount??(orderNet(order)-Number(order.deliveryFee||0)))),0),deliveryFee=sorted.reduce((sum,order)=>sum+(order.orderType==="delivery"?effectiveDeliveryAmounts(order).deliveryFee:Number(order.deliveryFee||0)),0),discount=sorted.reduce((sum,order)=>sum+Number(order.discountAmount||order.discount||0),0),total=sorted.reduce((sum,order)=>sum+orderNet(order),0),paidAt=paidDate(sorted[sorted.length-1])||paidDate(first)||new Date(),type=sorted.some(order=>order.orderType==="delivery")?"delivery":"table",customer=type==="delivery"?(first.recipientName||t("sales_report.receipt.delivery_customer")):t("sales_report.receipt.table_customer",{code:first.tableCode||"-"}),paymentKey=paymentMethodKey(first),itemCount=items.reduce((sum,item)=>sum+Number(item.qty||0),0),soldItemCount=items.filter(item=>item.isGift!==true).reduce((sum,item)=>sum+Number(item.qty||0),0),receipt={key:receiptGroupKey(first),orders:sorted,items,subtotal,deliveryFee,discount,total,paidAt,type,customer,paymentKey,itemCount,soldItemCount};receipt.code=receiptCode(receipt);return receipt}).sort((a,b)=>b.paidAt-a.paidAt)}
function selectedRange(){const now=new Date();if(state.period==="daily"){const value=els.reportDate.value||localDateKey(now);return{start:startOfDay(value),end:endOfDay(value),label:new Date(`${value}T12:00:00`).toLocaleDateString(locale,{dateStyle:"long"})}}if(state.period==="monthly"){const value=els.reportMonth.value||monthKey(now),[year,month]=value.split("-").map(Number),start=new Date(year,month-1,1),end=new Date(year,month,0,23,59,59,999);return{start,end,label:start.toLocaleDateString(locale,{month:"long",year:"numeric"})}}if(state.period==="yearly"){const year=Number(els.reportYear.value||now.getFullYear()),displayYear=isThai?year+543:year;return{start:new Date(year,0,1),end:new Date(year,11,31,23,59,59,999),label:t("sales_report.range.year",{year:displayYear})}}const startValue=els.startDate.value||localDateKey(now),endValue=els.endDate.value||startValue;return{start:startOfDay(startValue),end:endOfDay(endValue),label:t("sales_report.range.custom",{start:new Date(`${startValue}T12:00:00`).toLocaleDateString(locale),end:new Date(`${endValue}T12:00:00`).toLocaleDateString(locale)})}}
function applyFilters(){const range=selectedRange(),keyword=els.search.value.trim().toLowerCase();state.filteredReceipts = state.receipts.filter(receipt=>{if(receipt.paidAt<range.start||receipt.paidAt>range.end)return false;if(els.orderType.value!=="all"&&receipt.type!==els.orderType.value)return false;if(els.paymentMethod.value!=="all"&&receipt.paymentKey!==els.paymentMethod.value)return false;if(keyword){const haystack=[receipt.code,receipt.customer,...receipt.orders.map(order=>order.id),...receipt.items.map(item=>item.name)].join(" ").toLowerCase();if(!haystack.includes(keyword))return false}return true});state.page=1;renderAll(range)}
function summaryTotals(receipts){return{total:receipts.reduce((sum,receipt)=>sum+receipt.total,0),count:receipts.length,items:receipts.reduce((sum,receipt)=>sum+Number(receipt.soldItemCount??receipt.itemCount??0),0)}}
function renderSummary(){const totals=summaryTotals(state.filteredReceipts);els.totalSales.textContent=money(totals.total);els.receiptCount.textContent=formatNumber(totals.count,{maximumFractionDigits:0});els.averageReceipt.textContent=money(totals.count?totals.total/totals.count:0);els.soldItemCount.textContent=formatNumber(totals.items,{maximumFractionDigits:0})}
function renderBreakdown(container,entries){const max=Math.max(0,...entries.map(entry=>entry.value));container.innerHTML=entries.map(entry=>`<div class="breakdown-row"><span class="breakdown-label">${escapeHtml(entry.label)}</span><div class="breakdown-track"><div class="breakdown-fill" style="width:${max?(entry.value/max)*100:0}%"></div></div><span class="breakdown-value">${money(entry.value)} ${t("sales_report.units.baht")}</span></div>`).join("")||`<div class="empty-report">${t("sales_report.chart.no_sales")}</div>`}
function renderBreakdowns(){const typeTotals={table:0,delivery:0},paymentTotals={cash:0,promptpay:0,cod:0,other:0};state.filteredReceipts.forEach(receipt=>{typeTotals[receipt.type]+=receipt.total;paymentTotals[receipt.paymentKey]+=receipt.total});renderBreakdown(els.orderTypeSummary,Object.entries(typeTotals).map(([key,value])=>({label:orderTypeLabel(key),value})));renderBreakdown(els.paymentSummary,Object.entries(paymentTotals).map(([key,value])=>({label:paymentMethodLabel(key),value})))}
function chartBuckets(range){if(state.period==="daily"){setChartTitle(t("sales_report.sections.hourly_sales"));const buckets=Array.from({length:24},(_,hour)=>({key:hour,label:`${String(hour).padStart(2,"0")}:00`,value:0}));state.filteredReceipts.forEach(receipt=>{buckets[receipt.paidAt.getHours()].value+=receipt.total});return buckets}if(state.period==="yearly"){setChartTitle(t("sales_report.sections.monthly_sales"));const year=range.start.getFullYear(),buckets=Array.from({length:12},(_,month)=>({key:month,label:new Date(year,month,1).toLocaleDateString(locale,{month:"short"}),value:0}));state.filteredReceipts.forEach(receipt=>{buckets[receipt.paidAt.getMonth()].value+=receipt.total});return buckets}setChartTitle(t("sales_report.sections.daily_sales"));const buckets=[],cursor=new Date(range.start);while(cursor<=range.end){const key=localDateKey(cursor);buckets.push({key,label:cursor.toLocaleDateString(locale,{day:"numeric",month:"short"}),value:0});cursor.setDate(cursor.getDate()+1)}const index=new Map(buckets.map(bucket=>[bucket.key,bucket]));state.filteredReceipts.forEach(receipt=>{const bucket=index.get(localDateKey(receipt.paidAt));if(bucket)bucket.value+=receipt.total});return buckets}
function renderChart(range){const buckets=chartBuckets(range),max=Math.max(0,...buckets.map(bucket=>bucket.value));els.chartSubtitle.textContent=range.label;els.salesChart.classList.add("hourly-chart-scroll");els.salesChart.dataset.horizontalScroll="true";els.salesChart.innerHTML=buckets.map(bucket=>`<div class="chart-column" title="${escapeHtml(bucket.label)}: ${money(bucket.value)} ${t("sales_report.units.baht")}"><div class="chart-value">${bucket.value?money(bucket.value):""}</div><div class="chart-bar-wrap"><div class="chart-bar" style="height:${max?Math.max(2,(bucket.value/max)*100):2}%"></div></div><div class="chart-label">${escapeHtml(bucket.label)}</div></div>`).join("")}
function renderTopItems(){const totals=new Map();state.filteredReceipts.forEach(receipt=>receipt.items.filter(item=>item.isGift!==true).forEach(item=>{const key=String(item.name||t("sales_report.top_items.unnamed")),current=totals.get(key)||{qty:0,value:0};current.qty+=Number(item.qty||0);current.value+=Number(item.qty||0)*Number(item.price||0);totals.set(key,current)}));const ranked=[...totals.entries()].sort((a,b)=>b[1].qty-a[1].qty||b[1].value-a[1].value).slice(0,10);els.topItems.innerHTML=ranked.map(([name,value],index)=>`<div class="rank-row"><span class="rank-number">${index+1}</span><div><div class="rank-name">${escapeHtml(name)}</div><div class="rank-meta">${formatNumber(value.qty,{maximumFractionDigits:0})} ${t("sales_report.units.piece")}</div></div><div class="rank-value">${money(value.value)} ${t("sales_report.units.baht")}</div></div>`).join("")||`<div class="empty-report">${t("sales_report.top_items.empty")}</div>`}
function renderBestPeriod(range){const daily=new Map(),monthly=new Map();state.filteredReceipts.forEach(receipt=>{const day=localDateKey(receipt.paidAt),month=monthKey(receipt.paidAt);daily.set(day,(daily.get(day)||0)+receipt.total);monthly.set(month,(monthly.get(month)||0)+receipt.total)});const bestDay=[...daily.entries()].sort((a,b)=>b[1]-a[1])[0],bestMonth=[...monthly.entries()].sort((a,b)=>b[1]-a[1])[0],dayLabel=bestDay?new Date(`${bestDay[0]}T12:00:00`).toLocaleDateString(locale,{dateStyle:"long"}):"-",monthLabel=bestMonth?new Date(`${bestMonth[0]}-01T12:00:00`).toLocaleDateString(locale,{month:"long",year:"numeric"}):"-";els.bestPeriod.innerHTML=`<div class="best-period-card"><span class="menu-category">${t("sales_report.best.day")}</span><strong>${escapeHtml(dayLabel)}</strong><div>${money(bestDay?.[1]||0)} ${t("sales_report.units.baht")}</div></div><div class="best-period-card"><span class="menu-category">${t("sales_report.best.month")}</span><strong>${escapeHtml(monthLabel)}</strong><div>${money(bestMonth?.[1]||0)} ${t("sales_report.units.baht")}</div></div><div class="best-period-card"><span class="menu-category">${t("sales_report.best.period")}</span><strong>${escapeHtml(range.label)}</strong><div>${formatNumber(state.filteredReceipts.length,{maximumFractionDigits:0})} ${t("sales_report.units.receipts")}</div></div>`}
function printUrl(receipt){const ids=receipt.orders.map(order=>order.id);return ids.length>1?`/cashier/receipt/?orders=${encodeURIComponent(ids.join(","))}`:`/cashier/receipt/?order=${encodeURIComponent(ids[0])}`}
function renderReceipts(range){const start=(state.page-1)*state.pageSize,pageRows=state.filteredReceipts.slice(start, start + state.pageSize);els.receiptRows.innerHTML=pageRows.map(receipt=>`<tr><td><span class="receipt-code">${escapeHtml(receipt.code)}</span></td><td>${escapeHtml(formatTime(receipt.paidAt))}</td><td>${escapeHtml(orderTypeLabel(receipt.type))}</td><td>${escapeHtml(receipt.customer)}</td><td>${escapeHtml(paymentMethodLabel(receipt.paymentKey))}</td><td>${formatNumber(receipt.itemCount,{maximumFractionDigits:0})}</td><td class="number">${money(receipt.total)}</td><td><div style="display:flex;gap:6px;justify-content:flex-end"><button type="button" class="btn btn-sm" data-view-receipt="${escapeHtml(receipt.key)}"><i class="bi bi-eye" aria-hidden="true"></i><span>${t("sales_report.receipt.view")}</span></button><a class="btn btn-dark btn-sm" href="${printUrl(receipt)}" target="_blank" rel="noopener"><i class="bi bi-printer" aria-hidden="true"></i><span>${t("sales_report.receipt.print")}</span></a></div></td></tr>`).join("")||`<tr><td colspan="8"><div class="empty-report">${t("sales_report.receipt.no_results")}</div></td></tr>`;els.receiptResultCount.textContent=t("sales_report.receipt.result_count",{count:formatNumber(state.filteredReceipts.length,{maximumFractionDigits:0})});els.receiptRangeLabel.textContent=range.label;renderPagination()}
function renderPagination(){const pages=Math.ceil(state.filteredReceipts.length/state.pageSize);els.pagination.hidden=pages<=1;if(pages<=1){els.pagination.innerHTML="";return}const buttons=[];for(let page=1;page<=pages;page+=1){if(pages>9&&page>2&&page<pages-1&&Math.abs(page-state.page)>1){if(buttons.at(-1)!=="ellipsis")buttons.push("ellipsis");continue}buttons.push(page)}els.pagination.innerHTML=buttons.map(value=>value==="ellipsis"?'<span>…</span>':`<button type="button" class="${value===state.page?"active":""}" data-page="${value}">${value}</button>`).join("")}
function renderReceiptDetail(receipt){const itemRows=receipt.items.map(item=>{const isGift=item.isGift===true,name=`${item.name||t("sales_report.receipt.item_fallback")}${isGift?` ${t("sales_report.receipt.gift_suffix")}`:""}`,qty=formatNumber(Number(item.qty||0),{maximumFractionDigits:0}),lineTotal=Number(item.qty||0)*Number(item.price||0);return`<div class="receipt-item-row${isGift?" is-gift":""}"><div class="receipt-item-main"><div class="receipt-item-name">${escapeHtml(name)}</div><div class="receipt-item-meta">${qty} × ${money(item.price||0)}${isGift?`<span class="receipt-gift-badge">${t("sales_report.receipt.gift_badge")}</span>`:""}</div></div><strong class="receipt-item-total">${money(lineTotal)}</strong></div>`}).join("");els.detail.innerHTML=`<div class="receipt-detail-head"><div class="receipt-detail-identity"><span class="receipt-detail-label">${t("sales_report.receipt.number")}</span><div class="receipt-code">${escapeHtml(receipt.code)}</div></div><a class="btn btn-dark receipt-print-action" href="${printUrl(receipt)}" target="_blank" rel="noopener"><i class="bi bi-printer" aria-hidden="true"></i><span>${t("sales_report.receipt.print")}</span></a></div><div class="receipt-detail-facts"><div><span>${t("sales_report.receipt.date_time")}</span><strong>${escapeHtml(formatTime(receipt.paidAt))}</strong></div><div><span>${t("sales_report.receipt.channel")}</span><strong>${escapeHtml(orderTypeLabel(receipt.type))}</strong></div><div><span>${t("sales_report.receipt.customer_table")}</span><strong>${escapeHtml(receipt.customer)}</strong></div><div><span>${t("sales_report.receipt.payment")}</span><strong>${escapeHtml(paymentMethodLabel(receipt.paymentKey))}</strong></div></div><div class="receipt-detail-section-head"><strong>${t("sales_report.receipt.items_title")}</strong><span>${t("sales_report.receipt.item_count",{count:formatNumber(receipt.itemCount,{maximumFractionDigits:0})})}</span></div><div class="receipt-item-list">${itemRows||`<div class="empty-report">${t("sales_report.receipt.no_items")}</div>`}</div><div class="receipt-totals"><div class="receipt-total-row"><span>${t("sales_report.receipt.subtotal")}</span><strong>${money(receipt.subtotal)}</strong></div>${receipt.discount?`<div class="receipt-total-row"><span>${t("sales_report.receipt.discount")}</span><strong>-${money(receipt.discount)}</strong></div>`:""}<div class="receipt-total-row"><span>${t("sales_report.receipt.delivery_fee")}</span><strong>${money(receipt.deliveryFee)}</strong></div><div class="receipt-total-row grand"><span>${t("sales_report.receipt.total")}</span><strong>${money(receipt.total)} ${t("sales_report.units.baht")}</strong></div></div>`;els.dialog.showModal()}
function renderAll(range){renderSummary();renderBreakdowns();renderChart(range);renderTopItems();renderBestPeriod(range);renderReceipts(range)}
function setPeriod(period){state.period=period;document.querySelectorAll("[data-period]").forEach(button=>button.classList.toggle("active",button.dataset.period===period));document.querySelectorAll("[data-control]").forEach(control=>{control.hidden=control.dataset.control!==period});applyFilters()}
function initializeDates(){const now=new Date(),today=localDateKey(now);els.reportDate.value=today;els.reportMonth.value=monthKey(now);els.startDate.value=today;els.endDate.value=today;const currentYear=now.getFullYear();els.reportYear.innerHTML=Array.from({length:7},(_,index)=>currentYear-5+index).reverse().map(year=>`<option value="${year}"${year===currentYear?" selected":""}>${isThai ? year + 543 : year}</option>`).join("")}
document.querySelectorAll("[data-period]").forEach(button=>button.addEventListener("click",()=>setPeriod(button.dataset.period)));[els.reportDate,els.reportMonth,els.reportYear,els.startDate,els.endDate,els.orderType,els.paymentMethod].forEach(input=>input.addEventListener("change",applyFilters));els.search.addEventListener("input",applyFilters);els.pagination.addEventListener("click",event=>{const button=event.target.closest("[data-page]");if(!button)return;state.page=Number(button.dataset.page||1);renderReceipts(selectedRange());document.querySelector(".receipt-section")?.scrollIntoView({behavior:"smooth",block:"start"})});els.receiptRows.addEventListener("click",event=>{const button=event.target.closest("[data-view-receipt]");if(!button)return;const receipt=state.filteredReceipts.find(item=>item.key===button.dataset.viewReceipt);if(receipt)renderReceiptDetail(receipt)});document.querySelector("#closeReceiptDialog").addEventListener("click",()=>els.dialog.close());els.dialog.addEventListener("click",event=>{if(event.target===els.dialog)els.dialog.close()});initializeDates();
try{reportMenus=await dataService.listMenus()}catch(error){console.error("Unable to load menus for sales receipt gifts",error);reportMenus=[]}
const reportTenantId=String(dataService.getActiveShop()?.id||"").trim();
if(reportTenantId)setTenantId(reportTenantId);
dataService.subscribeOrders(orders=>{state.orders=orders||[];rebuildReceipts()});
watchRecords(RetailCollections.sales,sales=>{state.retailSales=sales||[];rebuildReceipts()},{sortBy:"createdAt",direction:"desc"});
